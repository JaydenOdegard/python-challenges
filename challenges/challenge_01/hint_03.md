### Hint 3 – Invalid Syntax: Capitalization and Operators

**Issue:**  
Wrote `If user_mood = 1` — capital “I” and single equals `=`

**Fix:**  
Python uses lowercase `if` and double equals `==` for comparison.

**Study:**  
- `if`/`else` syntax  
- Comparison vs assignment
